#include <stdio.h>
#include <string.h>

#define shstr(str) {str, sizeof(str)-1}

struct Short_String
{
    char buffer[512];
    u64 length;
};

int main(void)
{
    int a[10] = {0};
    int count_a = sizeof(a);

    int *b = a;
    int count_b = sizeof(b);

    // count_a != count_b;

    Short_String str = shstr("Hello, there!");
    if (str.buffer != 0) {
        printf("%d\n", str.length);
    }
}

int other_function(int x, int y)
{
    if (x == y) return 0;
    return 1;
}